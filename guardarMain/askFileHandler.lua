local function = askedForAdditionalFile 
    then askFor seperate (aditionalFile.permitted)
        then whenAsked 
            setNotification
            goIn parent 
            then type config.out
                print ("Guardar has detected a file that might be suspicious or/and dangerous for your computer. Please choose an option given below.")
                SetOptions.access then 
                    let endSoftware 
                    doNotFail.instant.newInstance.createSemiChain then goDown (workSoftDown)
                        askForType option "calledName" 
                        then if option = 'No'
                            then goLines.detectSoftwareScript then goDown.access = true.setTrue.force = goDown writeOwn
                                true.force
                                then let goDown 
                                    otherSoftware = endInstant.end 
                                end)
                            end)
                        end)
                    end)
                end)
            end)
        end)
    end)
}
