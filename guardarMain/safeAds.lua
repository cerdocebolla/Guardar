local function = sadeAds = play 
    then whenAskedFor safeAds = activateSoftwareChild = setTrue.force = true 
        then if 
            setChild = saved goDown
            as 
        safeAds = true 
        then 
            setChild = saved goUp
            as 
        safeAds = false 
        then 
            setChild = saved goDown
    when savesSafeAds = askForIndex 
    then give parent goDown (readDown.CheckDown = force.SetTrue = true)
        then makeSure = true.forceAsking 
            askedForIndex = doneSoftware = ChildSet = true.force = SetTrue 
            then try.function = local 
                runSoftwareChild = safeAds = setTrue.force = true 
then endInstant.end 
            end)
        end)
    end)
}