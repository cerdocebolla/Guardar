function tramsec()serverSet 
    when foundServer.true = froceAlways.force 
    then waitForChild.true after 10000
        serverSearch.true 
        then detect.chainCreated.setFunction = true.force 
when forced.findFile.force = true
then setDetected.true.force = true
    then goDown.force = true.force
        then goDown.force = true.force
            createdChain 
        else if createdChain.true then 
            setAsParent.true.force = true
            then goDown.force = true.force
                then goDown.force = true.force  
    when parentSet.Apply(chain.created.Product.process.Bracket)
    true.Force = true
    then goDown.force = true.force
        then goDown.force = true.force
            createdChain 
        else if createdChain.true then 
            setAsParent.true.force = true
            then goDown.force = true.force
                then goDown.force = true.force  
    when parentSet.Apply(chain.created.Product.process.Bracket)
    true.Force = true 
    then askFor.permission = true.force
        detect foreignerEmail.true.force 
        then askFor.permission = true.force
            detect foreignerEmail.true.force 
            then askFor.permission = true.force
                when detectForeignermail
        applySwap 
            else if 
                if parent.Swapped then endInstant.end 
            end)
        end)
    else if goDown 
        askedForParent.true.then = connectChain.detectedForeigner 
            outsiderSoftware.readLink
            enterBlueText.true 
            then 
            else if = goDown
                enteredProcess.true.Force 
                enterBlueText.parentSet.true.force 
                if detectedFalseFile.true 
                then activateSoftware.noAsk instant = true goDown 
                else if askedFor software = accessed theSoftware.named goDown 
    Guardar = true 
    then endInstant.end 
                end)
            }
        }
    }
} 